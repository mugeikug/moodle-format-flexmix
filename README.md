# format_flexmix

Moodleのコースフォーマットプラグインです。「ウィークリー(週ごと)」「トピック」「規定日以外の回
(補講などの特別回)」のセクションを、**1つのコースの中に混在**させられます。

標準の週次(Weekly)フォーマットは、各セクションの日付をセクションの並び順から自動計算しています。
そのため、途中に補講用のセクションを1つ挿入すると、それ以降の週の日付表示が全てズレてしまいます。
本プラグインでは、セクションごとに「種別(週 / トピック / 特別回)」を明示的に設定でき、週の日付は
**その種別が「週」であるセクションだけを数えて**計算するため、間にトピックや特別回のセクションを
挿入しても、後続の週の日付はズレません。

## 主な機能

- セクション編集パネル(セクション名の右にある編集メニューの「セクションを編集」)に、標準のセクション
  設定に加えて「セクションの種別」(週 / トピック / 特別回)と「セクションの日付」の項目が追加されます。
- **週**: ウィークリーフォーマットと同様に日付が自動計算されます(「セクションの日付」で個別に上書き
  可能)。
- **トピック**: トピックフォーマットと同様、日付を持たず「トピック N」という既定名になります。
- **特別回**: 通常の週次の流れに含まれない単発のセクションです。「セクションの日付」を設定すると
  既定のセクション名にその日付が表示されます。
- いずれの種別でも、コア標準の機能でセクション名を自由にリネームできます(リネームすると既定の
  自動生成名より優先されます)。

## 使い方

1. コース作成・編集画面の「コースフォーマット」で「週・トピック・特別回(混在)」を選択します
   (既存のウィークリーフォーマットのコースから移行することもできます)。
2. 通常どおりセクションを追加します。新しいセクションは既定で「週」種別になります。
3. トピックにしたい、または補講など任意日時にしたいセクションは、そのセクションの編集メニューから
   「セクションを編集」を開き、「セクションの種別」を変更します。
4. 特別回として日付を表示したい場合は「セクションの日付」を設定します。トピック・特別回セクションは
   セクション名を自分で付けることを想定しています。

## インストール

このディレクトリの内容を、Moodleの `course/format/flexmix/` にコピーまたはシンボリックリンクし、
サイト管理者としてサイトにログインしてプラグインのインストールを完了してください。

## 動作確認済み / 対象バージョン

`version.php` の `requires`/`supported` は Moodle 3.11〜4.1 系列を宣言していますが、これは互換性の
**宣言であり保証ではありません**。

Moodle 4.0でコースフォーマットの基底クラス(`format_base` → `\core_courseformat\base`)と出力層が
大きく変わっているため、本プラグインは以下の方法で単一コードベースのまま両対応しています。

- `lib.php`: `class_alias()` で、実行時に存在する方の基底クラス(3.11の`format_base`、または
  4.0+の`\core_courseformat\base`)を共通名にエイリアスしてから継承。
- レンダラー: 3.11用の`renderer.php`(コアweeksのレンダラーを継承)と、4.0+用の
  `classes/output/renderer.php`(同じくコアweeksの名前空間付きレンダラーを継承)を両方同梱。
  片方は各バージョンで読み込まれず無害です。

本番導入前に、対象ブランチのMoodleインスタンス(例: `moodle-docker` で `MOODLE_311_STABLE` と
`MOODLE_401_STABLE` を用意)に `course/format/flexmix` としてインストールし、実際のコース作成・
セクション編集画面から一連の操作を手動確認してください。特に「セクションを編集」パネルの開き方は
3.11と4.1でUIが大きく異なるため、`tests/behat/mixed_sections.feature` はその部分のUI操作をあえて
含めていません(コメント参照)。手動確認時に重点的に見るべきポイントは以下の通りです。

- ウィークリー相当のコースを作成し、途中に特別回セクションを挿入しても、後続の週の日付表示が
  保たれること。
- セクション編集パネルに「セクションの種別」「セクションの日付」が表示され、種別を「トピック」に
  すると日付欄が隠れること。
- セクション名を手動でリネームした場合、そちらが優先されること。

## 開発

```bash
composer create-project -n --no-dev --prefer-dist moodlehq/moodle-plugin-ci ci ^4
php ci/bin/moodle-plugin-ci install --plugin ./format_flexmix --db-host=127.0.0.1
php ci/bin/moodle-plugin-ci phpunit
php ci/bin/moodle-plugin-ci behat
```

## ライセンス

GNU GPL v3 or later
